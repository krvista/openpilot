# 라우트 00000007 (2026-09-08 출근, 빌드 208c286a) 분석

라우트 6 은 어제 저녁 19:00 (46 세그먼트, 뒤 26개가 재업로드), 라우트 7 은 오늘 07:57 출근길 40 세그먼트(GPS 37.54, 127.00).

## 1. 격리 코어·부하 (hardwared cpu_topology 스냅샷, 온로드 +120 s)

- **isolcpus = 6,7** 확정. 코어 4·5 는 격리가 아니라 일반 스케줄링 대상이다.
- IRQ 배치 실측: SPI(판다 링크) 630k → 코어 3, I2C(센서) 152k → 코어 1, GPU 29k → 코어 7, 카메라(ife·cci·cpas) → 코어 6,
  디스플레이(msm_drm) 7.7k(64 Hz) → 코어 5, 리스케줄 IPI 는 코어 0 에 198k(1.6 kHz). ufshc(스토리지) → 코어 0.
- 코어별 평균: 0 57.9 / 1 53.5 / 2 52.2 / 3 74.5 / 4 72.3 / 5 86.8 / 6 7.6 / 7 36.9 %. 어제와 같다.
- UI 30 fps 설정은 효과가 없었다: comma 4 는 vblank 로 프레임을 돌려 목표 fps 를 무시한다(ui 56 % 그대로).
  → 이번 커밋에서 vblank 두 번에 한 번만 그리도록 변경(실제 30 fps).

## 2. 지연·에코·이벤트

- carState→selfdriveState p50 2.6 ms, selfdriveState→carControl 1.4 ms(어제와 동일하게 개선 상태 유지), 판다 에코 p99 20 ms.
- 거부 에코 11 (부팅 9 + 단발 2), Lane Lines Lost 2회(3.05 s, 0.16 s), commIssue 는 부팅 직후 7 km/h 구간에서만(인수 경고 없음).
- 38-3 passive 탈출 59회: 탈출 프레임 와이어 게인 계단 최대 0.040 = 37a 캡. 어제 수정이 실차에서 확인됨.

## 3. 조향 품질 계측 (controlsState.angleFbInteg / steerCmdGapDeg, 첫 실측)

- 손 뗀 14,753 프레임: 7a 적분기 |중앙값| 0.88e-3, **상한(10e-4)에 붙은 시간 46 %**(30–50 km/h 50 %, 50–70 45 %),
  명령−핸들 갭 중앙값 2.79°, 그런데 적분기가 줄이려는 곡률 오차는 |중앙값| 0.14e-3(평균 −0.13e-3)로 작다.
- 해석: EPS 데드밴드 안의 상수 편향을 적분기가 2° 까지 감아 놓고 EPS 는 반응하지 않으므로, 명령이 핸들보다 2° 앞서
  서 있다가 EPS 가 반응하는 순간(커브 진입·운전자 접촉)에 한꺼번에 들어간다. 운전자 보정 직전 1 s 에 상한 상태였던
  비율 16 %(전체 9 %). 차선중심 오차는 0.03 m(p90 0.13)로 좋음 → 경로가 아니라 명령의 군더더기.
- 운전자 보정 에피소드 2.38/분(라우트 6 2.60, 5 2.92, v2 3.25) — 계속 감소 추세. 손 뗀 미추종 0.57/분.

## 4. 이번 커밋의 수정 (Phase 7a-6)

- 적분기 오차 데드밴드 0.2e-3 + 누설 τ 5 s. 커브 결손(0.8–1.0e-3)은 여전히 ~2 s 안에 상한 도달, 직진의 상수 편향은
  KI·τ·(e−db) 에서 멈춘다. 7a-5 가 되돌린 "과보정" 방향의 반대(트림 감소)라 inside-cut 재발 위험은 없다.
- 오프라인: 라우트 7 전체 손 뗀 구간 |트림| 0.88e-3 → 0.03e-3, 상한 시간 46 % → 0 %; 실제 controlsd 리플레이 seg 33
  명령−핸들 갭 1.23° → 0.46°(p90 2.93 → 1.41), 곡률 오차는 변화 없음(0.11e-3).
- 킬 스위치: LAT_FB_ERR_DEADBAND = 0, LAT_FB_LEAK_TAU = 0 → 7a-5 와 비트 동일.

## 5. 다음 로그에서 볼 것
- angleFbInteg 상한 비율(목표 < 5 %), steerCmdGapDeg 중앙값(목표 < 1°), 커브에서 트림이 여전히 차오르는지.
- ui CPU(목표 30 % 이하)와 코어 5 평균(목표 75 % 이하).
- 보정 에피소드 빈도와 >400 Nm 에피소드 수.
